{
  "$schema": "https://eips.ethereum.org/assets/eip-7730/erc7730-v2.schema.json",
  "context": {
    "$id": "Onvo InvoiceRegistry",
    "contract": {}
  },
  "metadata": {
    "owner": "Onvo",
    "contractName": "InvoiceRegistry",
    "info": {
      "url": "https://github.com/onvo/onvo"
    }
  },
  "display": {
    "formats": {
      "registerEmitter(address emitter,uint256 nullifierHash)": {
        "intent": "Register verified emitter (World ID validated off-chain)",
        "fields": [
          {
            "path": "emitter",
            "label": "Emitter wallet",
            "format": "addressName"
          },
          {
            "path": "nullifierHash",
            "label": "World ID nullifier hash",
            "format": "raw"
          }
        ]
      },
      "createInvoice(uint256 invoiceId,bytes32 invoiceHash_,address emitter,address recipient,uint256 amount,address token,string vatNumber,uint256 year,uint256 month)": {
        "intent": "Register verified invoice on-chain",
        "interpolatedIntent": "Invoice {year}-{month} — id {invoiceId} · total {amount} · VAT ID {vatNumber} · client {recipient} · pay to {emitter} · ref {invoiceHash_}",
        "fields": [
          {
            "path": "invoiceId",
            "label": "Packed invoice ID (F-…-YYYY-MM-seq)",
            "format": "raw"
          },
          {
            "path": "invoiceHash_",
            "label": "Payment reference (PDF hash)",
            "format": "bytes32"
          },
          {
            "path": "emitter",
            "label": "Issuer wallet (receives funds)",
            "format": "addressName"
          },
          {
            "path": "recipient",
            "label": "Client wallet",
            "format": "addressName"
          },
          {
            "path": "amount",
            "label": "Total amount (TTC, token units)",
            "format": "tokenAmount",
            "params": {
              "tokenPath": "token"
            }
          },
          {
            "path": "token",
            "label": "Currency (settlement token)",
            "format": "addressName"
          },
          {
            "path": "vatNumber",
            "label": "VAT identification number",
            "format": "text"
          },
          {
            "path": "year",
            "label": "Invoice year (calendar)",
            "format": "raw"
          },
          {
            "path": "month",
            "label": "Invoice month (1–12)",
            "format": "raw"
          }
        ]
      },
      "payInvoice(uint256 invoiceId)": {
        "intent": "Pay invoice — ERC-20 gross debit; contract sends platform fee to Onvo then net to issuer",
        "interpolatedIntent": "Pay invoice ID {invoiceId} (amount and token from contract storage)",
        "fields": [
          {
            "path": "invoiceId",
            "label": "Invoice ID",
            "format": "raw"
          }
        ]
      },
      "cancelInvoice(uint256 invoiceId)": {
        "intent": "Cancel pending invoice",
        "interpolatedIntent": "Cancel invoice ID {invoiceId}",
        "fields": [
          {
            "path": "invoiceId",
            "label": "Invoice ID",
            "format": "raw"
          }
        ]
      },
      "addAllowedToken(address token)": {
        "intent": "Allow settlement token",
        "fields": [
          {
            "path": "token",
            "label": "Token contract",
            "format": "addressName"
          }
        ]
      },
      "transferOwnership(address newOwner)": {
        "intent": "Transfer contract ownership",
        "fields": [
          {
            "path": "newOwner",
            "label": "New owner",
            "format": "addressName"
          }
        ]
      }
    }
  }
}
